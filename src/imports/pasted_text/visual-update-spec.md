THE3EYE GLOBAL VISUAL UPDATE

Use the current THE3EYE PRO screen as the visual reference for the entire application.

I want the font, colours, background, surfaces, buttons, borders, spacing feel and overall visual atmosphere of the current THE3EYE PRO screen applied consistently throughout the entire app.

The Premium screen is the visual reference. Do not redesign the Premium screen itself.

IMPORTANT: This is a visual update only. Existing functionality must remain unchanged.

Do not remove features.

Do not rebuild the map.

Do not replace the map.

Do not change navigation behaviour.

Do not change the existing information architecture.

Do not change routing logic.

Do not change existing functionality just because you are restyling a screen.

The current map is working again. The map implementation is LOCKED. Do not touch the map implementation.

Use the Premium screen's deep blue-black background throughout the app instead of generic black.

Use dark navy and blue-black surfaces for cards, panels, sheets and controls.

Use soft white for primary text.

Use muted blue-grey for secondary text and supporting information.

Use the existing The3eye Signal Green as the main accent.

Use the existing cool blue as the secondary accent.

Keep red for enforcement-related states, orange/amber for hazards, purple for accidents and yellow for roadwork.

Keep the colour system restrained and consistent. Do not introduce random new colours.

Use the same typography style as the Premium screen everywhere.

Major headings should use the same strong, bold typography seen in “Drive smarter. Stay focused.”

Important labels and actions should use strong semibold typography.

Supporting text should use the same clean, readable style.

Small metadata should use muted blue-grey.

The entire application should feel like it was designed from the beginning using the same design system as the Premium screen.

Apply this visual language to onboarding, login, home map, search, route preview, active navigation, report flow, report confirmation, profile, settings, vehicles, voice settings, permissions, Premium, Earn Pro and all other existing screens.

Cards should use dark navy surfaces with subtle borders and restrained corner radii.

Avoid bright white cards.

Avoid generic grey backgrounds.

Avoid excessive glassmorphism.

Avoid huge gradients and glowing effects.

Do not make normal screens look like advertisements for Premium.

Primary buttons should use Signal Green where appropriate.

Secondary buttons should use dark navy surfaces with subtle borders and soft-white text.

Destructive actions should use restrained red.

All icons should use the same clean original The3eye icon language.

Do not copy proprietary icons from Google Maps, Waze, Apple Maps, Mappls or other products.

Keep the existing The3eye vehicle icons and report icons, but make their colours and styling fit the new visual system.

The map itself must remain functional and unchanged.

The map must remain worldwide.

The map should initially center on the user's current location.

The map must remain behind the application UI.

The map must never cover controls.

All controls must remain visible at the normal mobile viewport.

Do not make UI elements appear only when the map is zoomed out.

Active navigation must keep its existing functionality.

Keep the three-line menu.

Keep the speaker control.

Keep the Re-center control.

Keep the route.

Keep the vehicle marker.

Keep live alerts.

Keep the speedometer and speed limit.

Keep ETA, remaining time and remaining distance.

Do not bring the current clock back into active navigation.

Visually restyle active navigation using the Premium colour and typography system.

Keep the restored richer report flow.

Do not simplify the report flow again.

Keep category selection, location confirmation, contextual report information, private photo evidence, submission and confirmation.

Keep report-during-navigation functionality.

Reporting during an active journey must remain a temporary layer over the journey.

After submitting a report during navigation, return to the exact same active navigation session.

Do not cancel, reset or restart the journey.

Do not lose the destination, route, vehicle, ETA, distance, progress or live alerts.

Back or Cancel from the report flow must return to the exact same active navigation session.

Keep the current Premium screen as the strongest visual reference.

Do not change its design.

Make every other screen visually move toward that same design language.

Before finishing, verify that the map still works, navigation still works, Re-center still exists, the speaker still exists, the microphone still works, reporting still works, report-during-navigation preserves the journey, End Journey still works, vehicles still work, Profile still works, Settings still works and Premium still works.

If a visual change causes the map or another existing feature to break, undo that change and preserve the working functionality.

Do not create a Markdown document. Do not create a separate design document. Apply these changes directly to the existing application.